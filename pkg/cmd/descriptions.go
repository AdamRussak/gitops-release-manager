package cmd

var rootLongDisc = "A CLI tool to dynamically create Release notes for GitOps projects based on a Tag and a commit Hash."
var releaseShortDisc = "Create Release Notes for a new version and create tags in Git and Azure DevOps Work-Items"
var releaseLongDisc = "Create the release notes for a new version between a tag and a commit hash."
var releaseReleaseDisc = "gitops-version release --repo-path /home/adam/<repo> --org <AzureDevOps orgID> --project <AzureDevOps project> --pat <AzureDevOps PAT > --hash <Git commit Hash> <path to git ssh keys> -v"
